# Task A: Full-input field prediction with MLPBaseline
task: field_prediction
model: mlp_baseline

data:
  split: canonical

model_args:
  in_channels: 2
  out_channels: 5
  scalar_dim: 5
  hidden_dim: 256

training:
  output_dir: results/task_a_mlp_baseline
  epochs: 50
  batch_size: 16
