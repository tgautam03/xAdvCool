# Task A: Full-input field prediction with DeepONet
task: field_prediction
model: deeponet

data:
  split: canonical

model_args:
  in_channels: 2
  out_channels: 5
  scalar_dim: 5
  latent_dim: 128

training:
  output_dir: results/task_a_deeponet
  epochs: 100
  batch_size: 4
  lr: 1.0e-3
